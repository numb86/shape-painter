import React from 'react';

import {SideMenuContainer} from '@src/shared/components/SideMenuContainer';

export const SideMenu = () => (
  <SideMenuContainer>Menu of Client Server</SideMenuContainer>
);
